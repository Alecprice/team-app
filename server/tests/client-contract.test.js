import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../..');const read=p=>fs.readFileSync(path.join(root,p),'utf8');
test('cloud client supports Neon managed auth, Data API sync, invites, docs, forms and E2EE messages',()=>{const c=read('client/cloud-entry.js');for(const token of ['createClient','NEON_AUTH_URL','NEON_DATA_API_URL','signUp.email','signIn.email','rpc(','app_team_update','app_invitation_create','app_join_create','app_document_upload','app_form_submit','app_message_send','scheduleSync','revision','uploadDocument','acknowledge','createConversation','wrapKeyFor','encryptMessage','enablePush','openAvailability','notification-preferences','data-direct-user','data-manage-form'])assert.ok(c.includes(token),`cloud client missing ${token}`);assert.ok(!c.includes('signIn.passkey'), 'managed Neon client should not advertise unsupported passkey sign-in');});
test('app exposes cloud runtime without breaking local first storage',()=>{const a=read('app.js');assert.match(a,/window\.TeamAppRuntime/);assert.match(a,/getActiveCloudPayload/);assert.match(a,/replaceCloudTeams/);assert.match(a,/TeamAppCloud\?\.scheduleSync/);assert.match(a,/meta\.cloud/);});
test('service worker excludes API from offline cache and handles push',()=>{const sw=read('sw.js');assert.match(sw,/url\.pathname\.startsWith\('\/api\/'\)/);assert.match(sw,/team-app-live-v1\.9\.0/);assert.match(sw,/showNotification/);});
