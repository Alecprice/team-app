# Team APP deployment status

**Not currently deployed.**

The stale Vercel binding was removed. Team APP is being migrated to Cloudflare Pages + Workers. See `DEPLOYMENT_RESET.md` for the clean deployment order.
